<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:label="@string/app_name_demo"
        tools:replace="android:label">
        <!-- نسخة الديمو ما فيها لعبة حقيقية: بس معرض الشاشات. -->
        <activity
            android:name="com.feudparty.app.MainActivity"
            tools:node="remove" />

        <activity
            android:name="com.feudparty.app.demo.DemoActivity"
            android:exported="true"
            android:keepScreenOn="true"
            android:theme="@style/Theme.FeudParty"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
